/*function firtLetter(str = "") {

    let maj = str.toLowerCase().slice(0, 1)
    let min = str.toLowerCase().substr(1)

    return `${maj}${min}`

}

console.log(firtLetter("ASSS AEEEi"))
*/

function kebanCase(str = "") {
    let finish;
    if (str.match(/-/)) {
        finish = str.replace
    }
}